-- 04-a-6000

Намерете PID и командата на процеса, който заема най-много виртуална памет в системата.

ps -e -o vsz= -o pid= -o cmd | sort -n | tail -n 1 | tr -s ' ' | sed 's/^ //g' | cut -d ' ' -f2-3

